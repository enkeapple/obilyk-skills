---
name: adr
description: Short user-typed entry point to record an architectural decision as an ADR. Alias for the writing-adrs skill.
disable-model-invocation: true
argument-hint: "<the decision to record, or ADRs to audit for drift>"
---

Use the `writing-adrs` skill. Treat the input below as the architectural decision
to record (or the ADRs to audit for drift).

Input: $ARGUMENTS
